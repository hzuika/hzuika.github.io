[INDEX](../)
このページではHTMLを記述する際に学んだこと等を載せていきます．<br/>

aタグ:リンクを張ることができる．  
```html
  <a href="URL">リンク</a>
```

codeタグ: ソースコードを記載できる．ただし，<>はタグとみなされるため，エスケープが必要．  
```html
  <code>ソースコード&lt;/code>
```

preタグ: 改行をそのまま記載できる．  
```html
  <pre>
    改行のある文章
    改行のある文章
  </pre>
```
